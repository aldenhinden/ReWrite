const { expect } = require('chai');
const { scrapePdf } = require('../../../src/back-end/webscrape/index.js');

function describe(scrapePdf, param2) {
    
}

describe('scrapePdf', () => {
    it('should return the text content of a PDF file', () => {
        // Your test code goes here
    });
});